# 字节跳动 ASIC验证工程师 一面挂经

> 来源：小红书 | 多次字节实习面试挂科，秋招再投

【一、异步 FIFO】

- 需要验证的核心场景

【二、AXI 协议】

- 通道分类
- burst 类型
- 窄带传输（narrow transfer）的原理

【三、手撕代码】

> 通过随机约束生成整数数组：
> - 数组长度 5~10 个元素
> - 有且仅有一个峰值元素（大于左右相邻元素）
> - 峰值左侧严格递增，右侧严格递减

【四、Python 验证】

- 是否了解 Python 验证框架 **cocotb**
- Python 中 `==` 与 `is` 的区别（`==` 比较值，`is` 比较对象身份）
- 对 Python"万物皆对象"理念的理解

【五、UVM】

- UVM 整体框架讲解
- VIP 的使用方式
- driver 向 sequence 返回 rsp 的方式，`get_rsp` 相关机制

【六、SystemVerilog】

- 关联数组的使用场景、核心优势与内置函数：
  - 适合存储稀疏数组元素
  - 地址空间较大的场景下仅为实际写入的元素分配空间，节省存储资源
  - 例如寄存器模型中寄存器空间定义常用关联数组
  - 常用内置函数：`.first(idx)`、`.delete(idx)` 等
