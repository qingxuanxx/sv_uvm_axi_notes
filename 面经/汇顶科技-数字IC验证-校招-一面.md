# 汇顶科技 IC验证岗 一面面经

> 来源：小红书 | 无自我介绍环节，面试官提前浏览简历，先介绍公司主营业务与部门情况

---

## 三部分内容

### 1. DEBUG 经历分享

### 2. Python 使用情况

- 用过的库
- 线程与进程的相关知识

### 3. 项目为核心穿插八股

- 最熟悉项目的验证环境、DUT 功能、提取的验证 Feature、整体验证方案
- UVM 环境包含的 agent、对接的 interface

### 覆盖率

- 不查看 vdb 文件获取覆盖率的方法
- 覆盖率未达 100% 的原因
- 图像随机化的方式与时机
- 权重配置、寄存器配置的随机化时机

### config_db（重点）

- set 操作的前后顺序
- **name 参数完整性对 `agt_*` 与 `agt_axis` 匹配的影响**（字段名模糊匹配的坑）
- sequence 获取组件 config 的方式
- 修改配置影响 axi-lite agent 的方法

### 其他

- sequence 的挂载方式与替代方案
- 参考模型采用 DPI-C 调用的实现方式
- 多协处理器场景下 DDR 带宽、CPU 端中断优先级的设计
- 使用的 CPU 型号

---

## 评价

> 面试官水平很高，会针对功能实现提问优化策略并引导作答，收获感很强；自身因遗忘项目细节反应稍慢。

## 关键收获

- config_db 字段名的**前缀匹配规则**：`agt_*` 会匹配所有 `agt_` 开头的字段——name 不写全可能配置错乱
- 覆盖率不看 vdb 文件 = 用命令行报告/日志提取
- 面试官会"引导作答"——不会直接判死，跟着思路走
